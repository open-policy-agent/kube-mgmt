suite: lint serviceaccount
templates:
  - fake.yaml
tests:
  - it: annotations not string
    set:
      serviceAccount:
        annotations:
          foo: 1
    asserts:
      - failedTemplate:
          errorPattern: "got number, want string"
